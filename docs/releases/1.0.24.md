## 主要修复

- 修复 iOS 发布流水线无法识别 Git 依赖 `libtorrent_flutter 2.0.1` 的问题。
- 构建前准备脚本现在按 `pubspec.lock` 的 `resolved-ref` 查找 Pub Git checkout，并继续兼容 hosted 包和已生成的 Flutter 插件链接。
- 保持 Android Torrent/Magnet 完成状态修复、多文件选择和真实真机验证结果不变。

## 构建验证

- Android APK/AAB、三平台桌面 GUI、三平台 CLI 已通过上一轮发布流水线的构建和产物校验。
- iOS 将在本版本流水线中重新执行 simulator、unsigned device 及相关产物门禁。
