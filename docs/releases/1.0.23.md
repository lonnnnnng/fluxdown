## 主要修复

- 修复 Android Torrent/Magnet 多文件任务的完成状态竞态：原生 libtorrent 可能已经完成并落盘，但 Flutter 任务卡仍停留在 `0 B` 或应用重启后回退为暂停。
- 移动端增加低频原生状态轮询，并串行化最终状态写回，确保文件落盘后才持久化 `finished` 和最终已下载大小。

## Torrent/Magnet 体验

- 真机 metadata 弹框展示完整文件名、目录路径、文件格式和文件大小。
- 取消文件选择不会创建任务；确认后才创建任务并保存选中的文件索引。
- Torrent/Magnet 任务列表使用 metadata 目录名，详情页标题为“资源详情”，只显示选中的文件及已下载大小。
- 补充 Android 真机真实 Torrent/Magnet 下载、重启恢复和无异常回归记录。

## 验证

- Redmi Note 8 Pro 真机 `wsvwypiz7xwslvl7`：Release APK `1.0.22 (23)` 完成 HTTP、Torrent、Magnet 前台流程；发布版本将由 `v1.0.23` 流水线重新构建并执行全平台门禁。
- Torrent 和 Magnet 均从空应用数据开始，真实下载 `payload.bin`，UI 显示 `8.0 MB / 8.0 MB` 并在进程重启后保持完成。
- `apps/mobile/test/mobile_torrent_test.dart` 通过。

完整证据与边界见 [下载验证状态](../download-verification.md) 和 [路线图](../roadmap.md)。
