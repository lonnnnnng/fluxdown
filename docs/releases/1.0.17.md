## 本次改动

- 修复 Windows 桌面 EXE 被链接为 Console 子系统的问题：Release 改为 GUI 子系统，启动应用不再同时出现命令行窗口，关闭控制台不会连带终止桌面应用。
- Windows Debug 构建继续保留控制台日志；`fluxdown.exe` CLI 继续使用 Console 子系统，命令行输出、脚本调用和退出码行为不变。
- Windows 启动时对可选 ed2k 后端执行的版本探测使用 `CREATE_NO_WINDOW`，避免后台探测控制台工具时短暂闪窗。
- 发布验证读取真实 PE32/PE32+ 文件头：桌面程序必须为 GUI 子系统，CLI 必须为 Console 子系统。旧版 `1.0.16` 桌面 EXE 已被新门禁按预期拒绝。

## 验证与边界

- 本地 Core 与桌面 Rust 测试 110 项通过，7 项需要外部协议环境的用例按原配置忽略；发布策略与 PE 文件头回归 21 项通过。
- `1.0.16` 正式 Windows 桌面 EXE 已核验为错误的 `Subsystem=3`，安装器哈希与 Release 附件一致，确认不是用户拿错安装包。
- [发行流水线 34223531407](https://github.com/lonnnnnng/fluxdown/actions/runs/34223531407) 的 10 个作业全部通过；Windows 桌面 EXE 为 `Subsystem=2`，CLI 为 `Subsystem=3`。正式 NSIS 安装器中的主程序与 CLI ZIP 内的程序均重新提取并通过 PE 核验，三平台 CLI 的 HTTP/Range 下载、队列暂停/恢复及 SHA-256 检查通过。
- 正式 11 个附件已重新下载，大小与 SHA-256 均匹配 manifest 和 GitHub API；macOS CLI 归档再次运行 smoke 通过，Android 元数据和三 ABI、APK v2 签名及 macOS DMG checksum 均已检查。完整证据见 [验证记录](../download-verification.md)。
- 本轮未做 Windows 前台快捷方式/托盘交互或真机完整协议下载验收，PE 门禁不能替代这些运行验证。
- macOS DMG 仍是标准 App Bundle；Linux DEB/RPM 的 `.desktop` 仍为 `Terminal=false`，本次修复不改变另外两个桌面平台。
- 当前未配置完整发行签名 secrets：本次 Android APK 实际使用 `Android Debug` 测试证书，Windows 未做 Authenticode，macOS 为 ad-hoc；iOS 仅提供 Actions 中的模拟器与未签名真机验证包，不提供普通用户可安装包。
