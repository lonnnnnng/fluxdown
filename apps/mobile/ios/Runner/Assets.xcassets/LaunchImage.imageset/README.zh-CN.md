# 启动屏资源

[English](README.md)

你可以替换本目录中的图片文件，用自己的资源自定义启动屏。

也可以打开 Flutter 项目的 Xcode workspace：

```sh
open ios/Runner.xcworkspace
```

然后在 Project Navigator 中选择 `Runner/Assets.xcassets`，把需要的图片拖入对应位置。
